#include <stdio.h>
#include <stdbool.h>

int main() {
    bool isSunny = true; // Declare a boolean variable

    if (isSunny) {
        printf("It's a sunny day!\n");
    } else {
        printf("It's not a sunny day.\n");
    }

    return 0;
}